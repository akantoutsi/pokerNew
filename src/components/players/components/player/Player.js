import React from 'react';

const Player = ({ cards, seq, isActive, cash, pot, potChanged, hisTurn, isDealer, isSmallBlind, isBigBlind}) => {
    return (
        <div>Player</div>
    );
}

export default Player;