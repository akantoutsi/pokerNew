import React   from 'react';
import Players from 'playersComponent/Players';

const Table = ({ cards }) => {
    return (
        <div>
            <Players />
        </div>
    );
}

export default Table;