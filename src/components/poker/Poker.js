import React   from 'react';
import Players from 'playersComponent/Players';

const Poker = ({ cards }) => {
    return (
        <div>
            <Players />
        </div>
    );
}

export default Poker;